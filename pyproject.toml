[project]
name = "bus-antsirabe-api"
version = "0.1.0"
description = "API Rest for retrieving bus line and travel information for Antsirabe"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["oxapy==0.6.8"]

[tool.setuptools]
packages = ["app"]
